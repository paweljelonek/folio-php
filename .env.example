APP_DEBUG=true
SHOW_RENDER_TIME=false

CONTENT_PATH=content
TEMPLATES_PATH=resources/templates

# Cache driver: file | null
CACHE_DRIVER=null
CACHE_DIR=var/cache
# Cache lifetime in seconds (0 = never expires)
CACHE_TTL=3600

# Docker ports (optional)
NGINX_PORT=8080
